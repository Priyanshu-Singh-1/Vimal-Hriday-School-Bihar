{
  "name": "vhs-admin-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate:local": "wrangler d1 migrations apply vhs-admin --local",
    "migrate:remote": "wrangler d1 migrations apply vhs-admin --remote"
  },
  "dependencies": {
    "hono": "^4.13.1",
    "jose": "^6.2.8"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.0",
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.1"
  }
}
