{
  "name": "vhs-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "seed:pilot": "node bin/seed-pilot-slots.mjs"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
